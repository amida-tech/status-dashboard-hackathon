{
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "calendar",
      "program": "${workspaceFolder}/backends/calendar/node_modules/.bin/sls",
      "args": [
        "invoke",
        "local",
        "-f",
        "calendar",
        "-d",
        "{}"
      ]
    }
  ]

}